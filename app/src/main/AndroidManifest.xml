<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.timmy.demopractice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.FirstActivity" />
        <activity
            android:name=".activity.SecondActivity"
            android:taskAffinity="com.tim.test"
            android:launchMode="singleInstance" />
        <activity android:name=".handler.HandlerActivity" />
        <activity android:name=".inflate.LayoutInflateActivity" />
        <activity android:name=".scrollview.ScrollViewActivity" />
        <activity android:name=".rv.RecyclerViewDemoActivity" />
        <activity android:name=".touch.TouchDemoActivity" />
        <activity android:name=".view.CustomeViewActivity" />
    </application>

</manifest>